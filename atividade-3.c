#include <stdio.h>

typedef struct{
    float altura;
    float largura;
    float profundidade;
} REGISTRO;

int main () {

    REGISTRO registros[10];

    // menu de opções
    // i (índices) de 0 a 9
    // registros[i].altura
    // registros[i].largura
    // registros[i].profundidade
}
